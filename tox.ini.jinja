[tox]
min_version = 4.0
env_list = {py{{python_version | regex_replace('\.', '')}}}
isolated_build = true
skip_missing_interpreters = true

[testenv]
deps =
    pytest
    pytest-cov
    botocore-stubber-recorder

commands = pytest tests
